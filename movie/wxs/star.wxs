function formRate(v){
  var lightStar = parseInt(v)/2;  //评分/2=星星数
  var star=[];
  for(var i=1;i <= 5;i++){
    if(i<=lightStar){
      star.push(1);
    }else{
      star.push(0);
    }
  }
  return star;
}
module.exports ={
  formRate:formRate
}